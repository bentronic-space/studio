id: td3-to-mpc
status: active

source:
  device: td3
  port: midi_out

target:
  device: mpc_one
  port: midi_in

signal:
  type: midi
  transport: midi_din

cable:
  id: midi_din_003

notes:
  - "TD-3 MIDI output is connected back to MPC MIDI input."
