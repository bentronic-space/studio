id: td3-to-mpc
status: active

source:
  device: td3
  port: midi_out

target:
  device: mpc_one
  port: midi_in

signal:
  type: midi
  transport: midi_din

cable:
  type: midi_din
  connector_source: din5
  connector_target: din5
  length_m: unknown

notes:
  - "TD-3 MIDI output is connected back to MPC MIDI input."
